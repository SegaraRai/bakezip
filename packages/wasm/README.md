# BakeZip WASM

This package contains the WebAssembly bindings for BakeZip, allowing the Rust core functionality to be used in web environments.

Run `pnpm build:wasm` to compile the Rust code to WebAssembly using `wasm-pack`. The output will be located in the `dist` directory.
